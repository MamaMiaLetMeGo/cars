<%= form_with(model: transmission, local: true) do |form| %>
  <% if transmission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transmission.errors.count, "error") %> prohibited this transmission from being saved:</h2>

      <ul>
        <% transmission.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :transmission %>
    <%= form.text_field :transmission %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
